# Placeholder schema only. Do not apply this file and do not store real values in Git.
apiVersion: v1
kind: Secret
metadata:
  name: okoscope-secrets
  namespace: okoscope
type: Opaque
stringData:
  database-url: "<postgresql-url>"
  postgres-password: "<postgres-password>"
  admin-credential: "<at-least-32-random-bytes>"
  webhook-encryption-key: "<64-hex-characters-encoding-32-random-bytes>"
  identity-token-key: "<at-least-32-random-bytes>"
---
# Create this Secret only from the one-time token returned by the Application API.
apiVersion: v1
kind: Secret
metadata:
  name: okoscope-application-credentials
  namespace: okoscope
type: Opaque
stringData:
  payment-api: "<oko_app_v1-token>"
