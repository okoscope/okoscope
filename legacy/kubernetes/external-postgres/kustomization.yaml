apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# Supply database-url in the externally owned okoscope-secrets Secret.
# This overlay deliberately contains no PostgreSQL workload or Service.

